# =============================================================================
# colouring-Pages - Environment Variables Template
# =============================================================================
# SKOPIUJ TEN PLIK DO .env I USTAW RZECZYWISTE WARTOŚCI
# NIE WKLEJAJ WARTOŚCI DO TEGO PLIKU - TO TYLKO PRZYKŁAD!
# =============================================================================

# =============================================================================
# === APP / WEB ===
# =============================================================================
NODE_ENV=development
PORT=3000
SITE_URL=http://localhost:3000  # Zmień na właściwą domenę w produkcji (np. https://kolorowanki-dla-dzieci.pl)
ADMIN_SECRET=twoj-tajny-klucz-tutaj

GENERATION_ENABLED=true

# =============================================================================
# === DATABASE (PostgreSQL) ===
# =============================================================================
# Neon, Supabase, lub inny PostgreSQL
# Format: postgresql://user:password@host:port/database?sslmode=require
DATABASE_URL=postgresql://user:password@host.neon.tech/dbname?sslmode=require

# =============================================================================
# === REDIS / QUEUE (BullMQ) ===
# =============================================================================
# Dla produkcji: użyj REDIS_URL (Upstash, Redis Enterprise, etc.)
# Format: redis://user:password@host:port
# Lokalnie: redis://localhost:6379
REDIS_URL=redis://localhost:6379

# =============================================================================
# === REDIS / QUEUE (Upstash) - alternatywa dla Upstash ===
# =============================================================================
# Opcjonalne - dla produkcji z kolejką zadań
UPSTASH_REDIS_REST_URL=https://xxx.upstash.io
UPSTASH_REDIS_REST_TOKEN=AXxxxx

# =============================================================================
# === STORAGE (Cloudflare R2) ===
# =============================================================================
# Opcjonalne - dla przechowywania obrazów
R2_ACCESS_KEY_ID=xxxxxxxxxxxxxxxxxxxxx
R2_SECRET_ACCESS_KEY=xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx
R2_BUCKET_NAME=colouring-pages-assets
R2_ACCOUNT_ID=xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx
R2_PUBLIC_URL=https://pub-xxx.r2.dev

# =============================================================================
# === AI (OpenAI) - OPCJONALNE ===
# =============================================================================
# Tylko jeśli używasz AI do generowania obrazów
# UWAGA: Trzymaj w tajemnicy! Nigdy nie wklejaj w front-end!
OPENAI_API_KEY=sk-xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx

# =============================================================================
# === LIMITY I KONTROLA KOSZTÓW ===
# =============================================================================
MAX_PAGES_PER_DAY=300
MAX_JOB_RETRIES=3
DAILY_TARGET=300
MAX_CONCURRENT_WORKERS=5
MAX_ASSET_SIZE_BYTES=10485760
MAX_QUEUE_SIZE=1000
JOB_TIMEOUT_SECONDS=30
BATCH_SIZE=300
DAILY_BUDGET_USD=10

# =============================================================================
# === SECURITY ===
# =============================================================================
API_RATE_LIMIT=100
JWT_SECRET=changeme_in_production

# =============================================================================
# === CRON ===
# =============================================================================
# Secret for cron job endpoints (must match in production!)
CRON_SECRET=changeme_in_production

# =============================================================================
# === LOGGING ===
# =============================================================================
LOG_LEVEL=info
